[project]
name = "06-batch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyspark>=4.2.0",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
